name: Generate Daily Podcast (US & TW)

on:
  schedule:
    - cron: "0 22 * * *"  # 每天台灣早上 6 點（美股）
    - cron: "0 8 * * *"   # 每天下午 4 點（台股）
  workflow_dispatch:

jobs:
  podcast:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        mode: [us, tw]

    env:
      PODCAST_MODE: ${{ matrix.mode }}
      MOONSHOT_API_KEY: ${{ secrets.MOONSHOT_API_KEY }}
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
      GROK_API_KEY: ${{ secrets.GROK_API_KEY }}
      GROK_API_URL: ${{ secrets.GROK_API_URL }}
      B2_KEY_ID: ${{ secrets.B2_KEY_ID }}
      B2_APPLICATION_KEY: ${{ secrets.B2_APPLICATION_KEY }}
      B2_BUCKET_NAME: daily-podcast-stk

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate Script
        run: python scripts/generate_script_kimi.py

      - name: Generate Audio
        run: python scripts/synthesize_audio.py

      - name: Upload to B2
        run: python scripts/upload_to_b2.py

      - name: Generate RSS
        run: python scripts/generate_rss.py

      - name: Commit and push RSS feed
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          
          git add docs/rss/podcast.xml || echo "✅ 沒有變更"
          git commit -m "更新 RSS Feed（${{ env.PODCAST_MODE }}）" || echo "✅ 無需提交"

          # 不要 pull，也不要 rebase，直接 push
          git push origin HEAD:main || echo "⚠️ 無變更可推送"
