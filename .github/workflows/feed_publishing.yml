name: Feed Generator

on:
  schedule:
    - cron: '35 22 * * *'  # 06:35 CST → 生成美股 RSS
    - cron: '40 6 * * *'   # 14:40 CST → 生成台股 RSS
  workflow_dispatch:

jobs:
  feed:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedgen

      - name: Generate US Feed
        if: github.event.schedule == '35 22 * * *'
        run: |
          echo "📡 執行推播員 (mode=us)"
          python scripts/feed_publisher.py --mode us

      - name: Generate TW Feed
        if: github.event.schedule == '40 6 * * *'
        run: |
          echo "📡 執行推播員 (mode=tw)"
          python scripts/feed_publisher.py --mode tw

      - name: Commit and Push RSS
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/rss/*.xml
          git commit -m "🔄 更新 RSS Feed ($(date '+%Y-%m-%d %H:%M'))" || echo "No changes to commit"
          git push